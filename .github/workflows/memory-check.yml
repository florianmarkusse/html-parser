name: Memory Check

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
    - uses: actions/checkout@v4

    - name: Install Valgrind
      run: |
          sudo apt-get update -q
          sudo apt-get install -y valgrind

    - name: Build
      run: ./build.sh -t -b

    - name: Memory check tests
      run: valgrind --leak-check=full --error-exitcode=1 build/tests/html-parser-tests-Release

    - name: Memory check benchmarks
      run: valgrind --leak-check=full --error-exitcode=1 build/benchmarks/html-parser-benchmarks-Release


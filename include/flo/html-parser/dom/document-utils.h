#ifndef FLO_HTML_PARSER_DOM_DOCUMENT_UTILS_H
#define FLO_HTML_PARSER_DOM_DOCUMENT_UTILS_H

#include "document.h"

const char *getText(node_id nodeID, const Document *doc,
                    const DataContainer *dataContainer);

#endif
